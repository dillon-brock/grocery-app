import { StyleSheet } from "react-native";
import { MenuItemStyle } from "../types/styles";

export default StyleSheet.create<MenuItemStyle>({
  container: {
    padding: 18
  },
  text: {
    fontSize: 20
  }
})