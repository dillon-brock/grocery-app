import { StyleSheet, ViewStyle } from "react-native"

type ShareScreenStyle = {
  pageContainer: ViewStyle;
}

export default StyleSheet.create<ShareScreenStyle>({
  pageContainer: {
    backgroundColor: '#F7EBE8',
    height: '100%'
  }
})